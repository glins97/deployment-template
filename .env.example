# Project Environment Variables
# Variables containing 'SECRET', 'KEY', or 'PASSWORD' → GitHub Secrets (encrypted)
# Other variables → GitHub Environment Variables (plaintext)

# Examples (uncomment and modify as needed):

# Secrets (encrypted in GitHub):
# JWT_SECRET=your_jwt_secret_key_here
# API_KEY=your_api_key_here
# DATABASE_PASSWORD=your_database_password

# Environment Variables (plaintext in GitHub):
# NODE_ENV=development
# DEBUG=true
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Database URL (contains secrets, will be encrypted):
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Add any other project-specific variables your application needs: